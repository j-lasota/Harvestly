type Verification {
    id: ID!
    shop: Shop!
    user: User!
}

extend type Query {
    verifications: [Verification]
    verificationById(id: ID!): Verification
}

extend type Mutation {
    createVerification(shopId: ID!, userId: ID!): Verification
    deleteVerification(id: ID!): Boolean
}