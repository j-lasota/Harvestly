spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sumy
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop  # important for tests
    show-sql: true
